language: "en"
intents:
  MassPlayMediaOnMediaPlayer:
    data:
      - sentences:
          - "(play|listen to) {query} <in> [the] (<name>|[<player_devices> <in> the] <area> [<in> the <player_devices>])"
          - "<in> [the] (<name>|[<player_devices> <in> the] <area> [<in> the <player_devices>]) (play|listen to) {query}"
        expansion_rules:
          player_devices: "(speaker[s]|[media] player[s])"
lists:
  query:
    wildcard: true
